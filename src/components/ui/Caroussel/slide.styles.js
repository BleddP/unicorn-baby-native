import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        width: "90%",
        height: '100%',
        resizeMode: "cover",
        borderRadius: 15,
    },
})

export default styles